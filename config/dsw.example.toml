# Copy this file to .local/dsw.toml and fill only values confirmed by the lab.
# .local/dsw.toml is ignored by Git.

host = ""
user = ""
network_mode = ""
shared_data_root = ""
announcement_channel = ""
max_persistent_gpus = 0

[defaults]
gpu_count = 1
minimum_free_disk_percent = 20
require_cuda_visible_devices = true
allow_kill_other_users_processes = false
require_persistent_service_approval = true

